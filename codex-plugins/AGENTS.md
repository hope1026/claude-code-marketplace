# Repository Guidelines

## Structure

- Codex skills live under `.agents/skills/`
- Each skill keeps its own `SKILL.md` and optional `agents/` metadata

## Editing

- Preserve existing skill names because users may invoke them directly
- Keep relative paths inside each skill package portable
- Avoid absolute machine-specific paths in docs or examples
